name: mobile
description: "Aplicación móvil HACO"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2                # Para manejar estado
  http: ^1.2.2                    # Para consumir API
  flutter_native_splash: ^2.3.9   # Splash screen
  shared_preferences: ^2.3.2      # Almacenamiento local
  flutter_secure_storage: ^9.0.0  # Almacenamiento seguro
  rename_app: ^1.6.5
  flutter_launcher_icons: ^0.13.1 # Para cambiar el ícono de la app
  google_fonts: ^6.1.0          # Fuentes de Google
  geolocator: ^11.0.0          # Geolocalización

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/icon.png"
  remove_alpha_ios: true

flutter_native_splash:
  color: "#F1F1F1"   # Fondo gris
  image: assets/images/unilogo.png
  android: true
  ios: true
  web: false

  android_12:
    image: assets/images/unilogo.png
    icon_background_color: "#F1F1F1"